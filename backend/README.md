VTU Examination Results System - Backend
----------------------------------------

This backend simulates a real API using a static JSON file.

File:
- sample-results.json : contains mock VTU results for testing.

Usage:
- The frontend fetches this JSON file to display results.
- For local testing, run: npx http-server . -p 8080
